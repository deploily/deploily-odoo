<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="deploily_signup_fields" inherit_id="auth_signup.fields">
        <xpath expr="//div[@class='mb-3 field-confirm_password']" position="after">
            <div class="mb-3 field-terms d-flex align-items-start gap-2">
                <input type="checkbox"
                    name="terms_conditions"
                    id="terms_conditions"
                    class="form-check-input mt-1"
                    required="required" />

                <label for="terms_conditions" class="form-check-label text-muted small"> By creating an account, you confirm that you have read and agree to our <a t-att-href="terms_conditions_page" target="_blank">Terms of Use</a> and <a t-att-href="privacy_policy_page" target="_blank">Privacy
                    Policy</a>, and you consent to the processing of your personal data in accordance with <strong>Law 18-07 on the Protection of Individuals</strong>. </label>
            </div>
        </xpath>
    </template>
    <template id="deploily_signup" inherit_id="auth_signup.signup" name="Sign up">
        <xpath expr="//div[@class='text-center oe_login_buttons d-grid pt-3']" position="before">
            <t t-call="deploily_signup.recaptcha" />
        </xpath>
    </template>
    <template id="deploily_signup.recaptcha" name="Add reCAPTCHA to Signup">
        <div class="mb-3 o_recaptcha" style="margin-top: 10px;">
            <div class="g-recaptcha" name="recaptcha" t-att-data-sitekey="website.get_recaptcha_site_key()" t-attf-data-callback="{{ callback_name or 'signup' }}"></div>
        </div>
        <script src="https://www.google.com/recaptcha/api.js" async="1" defer="1"></script>

    </template>
</odoo>